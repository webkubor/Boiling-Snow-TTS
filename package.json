{
  "name": "qwen3-ttsboiling-snow-tts",
  "private": true,
  "scripts": {
    "ai:check": "bash scripts/verify-ai-init.sh",
    "hooks:install": "bash scripts/setup-git-hooks.sh",
    "api:dev": ".venv/bin/python -m uvicorn web_api:app --reload --host 0.0.0.0 --port 8000",
    "web:dev": "npm --prefix webui run dev",
    "web:build": "npm --prefix webui run build",
    "design:batch": ".venv/bin/python scripts/run_design_batch.py",
    "design:batch:dry": ".venv/bin/python scripts/run_design_batch.py --dry-run"
  }
}
